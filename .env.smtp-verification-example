# SMTP Email Verification Configuration Example
# Copy these settings to your .env file to enable SMTP verification

# Enable SMTP email verification (default: false)
# WARNING: Use with caution in production - can be resource intensive
MAIL_SMTP_VERIFICATION_ENABLED=false

# SMTP connection timeout in seconds (default: 10)
MAIL_SMTP_TIMEOUT=10

# Maximum number of MX hosts to try (default: 3)
MAIL_SMTP_MAX_MX_HOSTS=3

# Mail configuration for SMTP verification
# These should match your actual mail settings
MAIL_FROM_ADDRESS="noreply@yourdomain.com"
MAIL_FROM_NAME="Your Application"

# Example production configuration
# MAIL_SMTP_VERIFICATION_ENABLED=true
# MAIL_SMTP_TIMEOUT=15
# MAIL_SMTP_MAX_MX_HOSTS=2

# IMPORTANT NOTES:
# 1. SMTP verification connects to recipient mail servers to check email existence
# 2. Some mail servers may block or throttle verification attempts
# 3. Can cause delays in email validation (10-15 seconds per email)
# 4. May be considered spam-like behavior by some providers
# 5. Recommended to use caching and rate limiting in production
# 6. Test thoroughly before enabling in production

# Testing commands:
# php artisan email:test-smtp user@gmail.com
# php artisan email:test-smtp user@domain.com --enable-smtp
# php artisan email:test-smtp user@domain.com --enable-smtp --timeout=15
